// Browser Supabase client factory.
//
// TODO(step #2 — auth): implement with `createBrowserClient` from
// `@supabase/ssr` using NEXT_PUBLIC_SUPABASE_URL / NEXT_PUBLIC_SUPABASE_ANON_KEY.
export function createClient(): never {
  throw new Error("Supabase browser client not implemented yet (step #2).");
}
