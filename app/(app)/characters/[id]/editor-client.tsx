"use client";

// TODO(step #5): the character editor (react-hook-form + zod). Panels:
// BasicInfoForm, AttributesPanel, SkillsTable, TraitsTable, XpSummary,
// NotesEditor. Explicit Save → update_character RPC with expected_version.
export function EditorClient() {
  return <div>Editor (TODO step #5)</div>;
}
